// Activity type to Pexels photo mapping (curated)
// Now uses centralized category configuration
import { getCategoryPhoto } from '@/utils/categories';
export const getActivityPhoto = getCategoryPhoto;
